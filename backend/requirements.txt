# Core web framework
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
pydantic>=2.7.0
websockets>=12.0

# Database
sqlalchemy>=2.0.0

# HTTP client (plugin webhook dispatcher)
httpx>=0.27.0

# STT — faster-whisper + silero-vad VAD
faster-whisper>=1.0.0
silero-vad>=5.1.0
torch>=2.3.0
numpy>=1.26.0

# Speaker diarisation — requires HuggingFace token; configure separately
# pyannote.audio>=3.3.0

# Vector store
chromadb>=0.5.0

# LLM client — Ollama exposes an OpenAI-compatible API
openai>=1.30.0

# Multipart form / file upload (required by FastAPI UploadFile)
python-multipart>=0.0.9

# Config
python-dotenv>=1.0.0
